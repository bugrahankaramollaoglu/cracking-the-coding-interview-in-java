package data_structures.string;

public class Strings {

    public static void main(String[] args) {

        String str1 = "bugra"; // stored in stringpool

        String str2 = new String("bugra"); // stored in heap

        // javada stringlerin bir sürü metodu vardır, bunlara s.() ile erişebilirsin

        /*Strings cannot be modified once created. Any operation that
        appears to change a string actually creates a new object.
        Use StringBuilder or StringBuffer for mutable strings. */

//        StringBuilder sb = new StringBuilder("asde");
//        sb.append();
//        sb.insert();
//        sb.reverse();


    }
}
